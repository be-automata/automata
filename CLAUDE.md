@AGENTS.md

<!-- somnio:harness:begin -->
> This block is regenerated by `/harness-init` on every run — edits made inside it are lost
> the next time it runs. Write anything you want kept in the section above ("What this repo
> is") instead.

## Stacks detected

node

## Rules

Rule bodies are never inlined here — only the index. Each row loads lazily, only when the
matching path is touched (see `optimize-context-rules`).

_No rules installed._

## Verification commands

| command | what it proves |
|:--|:--|
| npm ci | — |
| npm test | — |
| npm run build | — |
<!-- somnio:harness:end -->